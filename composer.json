{
    "name": "ds-labs/redaktor",
    "description": "Provide api revision to your api.",
    "require": {
        "php": "^7.2"
    },
    "require-dev": {
        "phpspec/phpspec": "^5.1"
    },
    "autoload": {
        "psr-4": {
            "DSLabs\\Redaktor\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "spec\\DSLabs\\Redaktor\\": "spec/"
        }
    },
    "scripts": {
        "test": "phpspec run --format=dot"
    }
}
